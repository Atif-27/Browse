import { Outlet } from "react-router-dom";

const ChannelPage = () => {
  return (
    <div>
      ChannelPage
      <div>
        <Outlet />
      </div>
    </div>
  );
};

export default ChannelPage;
